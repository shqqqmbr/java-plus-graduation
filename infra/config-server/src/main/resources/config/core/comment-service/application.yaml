spring:
  datasource:
    url: jdbc:postgresql://comment-db:5432/comment
    username: comment
    password: comment
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        default_schema: public
        format_sql: false

  logging:
    level:
      ru.practicum.ewm: DEBUG